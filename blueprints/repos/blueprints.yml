blueprints:
        - stackname: '{var:ProjectName}-repositories'
          template: codecommit.json
          parameters:
                  RepositoryName: '{var:ProjectName}-code'
                  EcrName: '{var:ProjectName}'
